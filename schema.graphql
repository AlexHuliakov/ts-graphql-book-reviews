type Query {
    users(query: String): [User!]!
}

input CreateUserInput {
    name: String!
    email: String!
}

type User {
    id: ID!
    name: String!
    email: String!
}

type Mutation {
    createUser(data: CreateUserInput): User!
}
